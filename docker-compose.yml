services:
  mongodb:
    image: mongo:7
    container_name: seller-feedback-intelligence-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
